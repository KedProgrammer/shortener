

<% if @url.errors.any? %>
	$('.errors').remove()
	$('.main').prepend("<%= j (render 'errors') %>");
<% else %>
	$('.errors').remove()
	$('tbody').html("<%= j (render 'table') %>");
	$('.modal-content').html("<%=  j (render 'modal') %>")
	$('#exampleModal').modal()
	
<% end %>